# Canonical public Brama ingress on the registered Linux service host.
# BRAMA_UPSTREAM is the loopback listener assembled by the deployment workflow.
https://brama.wisent.ai {
    tls {
        issuer acme {
            disable_http_challenge
        }
    }
    reverse_proxy {$BRAMA_UPSTREAM}
}
